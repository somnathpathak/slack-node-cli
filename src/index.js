import { postMessage } from "./lib/slack";

export default function () {
    // make changes as per the module requirements

    return {
        sendSlackMessage: postMessage
        // Add other methods...
    }
};

